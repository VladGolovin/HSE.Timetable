import floatToTime from 'ember-app/utils/float-to-time';
import { module, test } from 'qunit';

module('Unit | Utility | float to time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = floatToTime();
  assert.ok(result);
});
